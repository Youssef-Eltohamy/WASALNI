import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Color(0xFF1B998B);
  static const Color primaryDark = Color(0xFF147A6E);
  static const Color primaryLight = Color(0xFF4FB7AB);

  // Accents
  static const Color accent = Color(0xFFFFB845);

  // Surfaces
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F4F3);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF5A6168);
  static const Color textHint = Color(0xFF9AA0A6);

  // Feedback
  static const Color success = Color(0xFF2E7D5B);
  static const Color warning = Color(0xFFE8A33D);
  static const Color error = Color(0xFFD1495B);
  static const Color info = Color(0xFF3D7DCA);

  // Borders & dividers
  static const Color border = Color(0xFFE3E6E5);
  static const Color divider = Color(0xFFEDEFEF);
}
